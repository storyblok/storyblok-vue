class MockStoryblokClient {
  get = vi.fn();

  // Add any other methods from the StoryblokClient class if needed, e.g.:
  // post = jest.fn();
  // put = jest.fn();
  // delete = jest.fn();
}

export default MockStoryblokClient;
